export type Video = {
  id: string
  title: string
  url: string
  thumbnail?: string
  description?: string
  slug: string
}

export type NavLink = {
  label: string
  href: string
}

export type Page = string  
